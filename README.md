# Diabetes-Detection-using-Data-Science
Built a machine learning model to predict diabetes using clinical data. Performed data cleaning, EDA, feature engineering, and trained multiple algorithms including Logistic Regression, Random Forest, and SVM. Tuned hyperparameters and selected the best model, creating a reliable prediction pipeline for healthcare use.
